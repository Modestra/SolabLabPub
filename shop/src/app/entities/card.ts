export interface CardProduct {
    name: string,
    cost: number,
    location: string,
    time: string
}

export interface Category {
    category_id: string,
    name: string,
    parentid: string
}